
            let timeInMinutes = 10;
            let timeInSeconds = timeInMinutes * 60;
            
            // To print the minutes and converted seconds
            let data = `minutes ${timeInMinutes} converted to seconds ${timeInSeconds}`;
            console.log(data);
